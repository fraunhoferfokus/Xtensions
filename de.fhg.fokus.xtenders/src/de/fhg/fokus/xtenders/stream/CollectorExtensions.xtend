package de.fhg.fokus.xtenders.stream

import java.util.stream.Collectors
import java.util.Arrays

class CollectorExtensions {
	
	/// TODO >>> operator for Iterable/Collection/Stream or single element
	// TODO << operator on Collector, procuces CollectOperation.
	//       then << operator for more elements and << finish
	
	// Example:
	// val c = Collectors.reducing(0,[a,b|a+b])
	// val foo = c << 1 << 3 << 6 << finish
}